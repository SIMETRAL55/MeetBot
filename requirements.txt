# ============================================================================
# MeetBot - Python Dependencies (pinned)
#
# Generated from a working environment. All runtime packages are pinned with
# exact versions (==) for reproducible installs.
#
# Install order for a new environment:
#   1. Install system deps: ffmpeg, libsndfile1, build-essential, cmake
#   2. Install PyTorch for your hardware (see torch section below)
#   3. pip install -r requirements.txt
#   4. pip install -e .
# ============================================================================

# ============================================================================
# Core Dependencies
# ============================================================================
python-dotenv==1.2.1          # Load environment variables from .env
pydantic==2.12.5              # Data validation using Python type annotations
pydantic-settings==2.12.0     # Settings management in Pydantic
requests==2.32.5              # HTTP client library
tqdm==4.67.2                  # Progress bars
toolz==1.1.0                  # Functional programming utilities

# ============================================================================
# Web Application
# ============================================================================
nicegui==3.8.0                # Python-based web UI framework (includes FastAPI + Uvicorn)
sqlalchemy==2.0.46            # ORM for SQLite / PostgreSQL
alembic>=1.13.0               # Database schema migrations
bcrypt==5.0.0                 # Password hashing
slowapi==0.1.9                # Rate limiting middleware for FastAPI/Starlette
limits==5.8.0                 # Rate limit state stores (used by slowapi)
python-magic==0.4.27          # MIME-type detection via libmagic

# PostgreSQL support (optional — only needed if DATABASE_URL points to PostgreSQL)
# pip install psycopg2-binary
# psycopg2-binary>=2.9.9


# ============================================================================
# Audio Processing & Transcription
# ============================================================================
openai-whisper==20250625      # OpenAI's Whisper ASR model (CPU/GPU)
faster-whisper>=1.0.0         # CTranslate2-based Whisper (4-6x faster, 50% less VRAM)
pydub==0.25.1                 # Audio file format conversion
ffmpeg-python==0.2.0          # Python wrapper for ffmpeg — only version on PyPI

# Note: ffmpeg binary must be installed separately:
#   Ubuntu/Debian: sudo apt-get install ffmpeg
#   macOS: brew install ffmpeg
#   Windows: Download from https://ffmpeg.org/download.html


# ============================================================================
# Speaker Diarization
# ============================================================================
pyannote.audio==3.2.0         # Speaker diarization and verification

# PyTorch — install separately BEFORE this requirements file to choose the
# correct hardware variant.  The lines below are excluded by the Dockerfile's
# builder stage (see RUN grep -v … in Dockerfile) and are provided here only
# to document the expected version for CPU installs.
#
# CPU only (default):
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 \
#       --index-url https://download.pytorch.org/whl/cpu
#
# CUDA 11.8:
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 \
#       --index-url https://download.pytorch.org/whl/cu118
#
# CUDA 12.1 (used in this environment):
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 \
#       --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1


# ============================================================================
# Embeddings & Vector Stores (RAG)
# ============================================================================
sentence-transformers==5.2.2  # State-of-the-art sentence embeddings
chromadb==1.4.1               # Vector database for document retrieval
numpy==1.26.4                 # Numerical computing (numpy<2 required by embeddings)


# ============================================================================
# LangChain & RAG Ecosystem
# ============================================================================
langchain==1.2.7              # Framework for building with LLMs
langchain-core==1.2.15        # Core types and interfaces
langchain-community==0.4.1    # Community integrations (includes Chroma fallback)
langchain-huggingface==1.2.0  # HuggingFace integration for LangChain
# langchain-chroma is optional: code falls back to langchain_community if absent.
# Install with: pip install langchain-chroma
# langchain-chroma>=0.1.0


# ============================================================================
# LLM & HuggingFace Integrations
# ============================================================================
huggingface-hub==0.36.0       # Download and interact with HuggingFace models
transformers==4.57.6          # HuggingFace Transformers library
autoawq                       # AWQ 4-bit quantized model inference (GEMM kernel)
accelerate                    # HuggingFace Accelerate for device_map support


# ============================================================================
# PageIndex (vectorless LLM-based retrieval — optional)
# PageIndex is NOT pip-installable. Clone the repo and add to PYTHONPATH:
#   git clone https://github.com/VectifyAI/PageIndex.git vendor/pageindex_repo
#   echo "$PWD/vendor/pageindex_repo" >> venv/lib/python3.12/site-packages/pageindex.pth
# ============================================================================
openai>=1.50.0                # OpenAI-compatible API client (used by PageIndex)
tiktoken>=0.7.0               # Token counting for PageIndex
pyyaml>=6.0                   # YAML config loading for PageIndex


# ============================================================================
# Development & Testing (not needed in production)
# ============================================================================
pytest==9.0.2                 # Testing framework
pytest-asyncio==1.3.0         # Async test support
